{
  "name": "taskmaster-api",
  "version": "1.0.0",
  "description": "Cadastro, login, autenticação via token (JWT)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "tsx src/server.ts",
    "start:watch": "tsx watch src/server.ts",
    "start:dist": "npm run dist && node dist/index.js"
  },
  "author": "Ricardo Tonholli",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/express-validator": "^3.0.2",
    "tsup": "^8.5.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "dotenv": "^17.0.0",
    "express": "^5.1.0",
    "express-validator": "^7.2.1"
  }
}
